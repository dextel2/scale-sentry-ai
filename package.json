{
  "name": "@dextel2/scale-sentry-ai",
  "version": "1.0.0",
  "description": "GitHub Action that uses OpenAI to forecast scalability risks from pull request diffs.",
  "main": "dist/index.js",
  "license": "MIT",
  "scripts": {
    "build": "tsc --project tsconfig.json && ncc build lib/index.js -o dist --source-map",
    "pretest": "npm run build",
    "test": "node scripts/smoke-test.js"
  },
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/github": "^6.0.1"
  },
  "devDependencies": {
    "@types/node": "^24.7.0",
    "typescript": "^5.9.3",
    "@vercel/ncc": "^0.38.4"
  },
  "engines": {
    "node": ">=18"
  }
}
